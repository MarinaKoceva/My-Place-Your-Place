import SurroundingsSearch from "./SurroundingsSearch";

export default function SurroundingsPage() {
  return (
    
    <section style={{ marginTop: "2rem" }}>
      <h2 style={{ textAlign: "center", color: "#fff" }}>Find places by surroundings</h2>
      <SurroundingsSearch />
    </section>
  );
}